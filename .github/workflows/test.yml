name: Test
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  dagger:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test
        id: test
        run: make test
